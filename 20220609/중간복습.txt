2022 06 09 목요일 

23 에러캐치

24 중간복습

className => 문자열

style => 객체, 카멜케이스, className보다 먼저

폼다루기1
1. onSubmit => event.preventDefault()
2. event.target.elements => input의 value를 직접 관리

폼다루기2
1. validation => onChange
2. controlled => input의 value를 직접 관리

에러다루기
Error Boundary => Catch Error해서 보여주기

Fallback => Error가 났을떄 보여줄 컴포넌트

24,25 
key와 리렌더링 알아보기2
재사용 => 키를 제대로줘야 재사용가능
제대로준다 => 중복없고, 바뀌지않는.